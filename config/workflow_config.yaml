model:
  provider: openai
  name: gpt-4.1-mini
  api_key_env_var: OPENAI_API_KEY
  max_output_tokens: 1200

generation:
  output_language: typescript

execution:
  mode: mock
  require_approval: false

action_fields:
  source: static
  # To use real platform fields later:
  # source: file
  # paths:
  #   - /Users/mayank.sultania/Documents/Company/Projects/Dexda-EventsProcessor/global_fields.json
  extra_fields: []
  wildcard_prefixes: []

action_service:
  submit_mode: disabled
  # To enable real submit later:
  # submit_mode: http
  # base_url: http://action:8447
  # submit_endpoint: /action/group
  # auth_token_env_var: ACTION_SERVICE_TOKEN
  timeout_seconds: 10

tools:
  enabled:
    - event_tool
    - alert_tool
    - servicenow_tool
    - access_tool
